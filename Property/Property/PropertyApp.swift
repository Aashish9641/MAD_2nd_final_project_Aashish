import SwiftUI // Importing the swift UI framework

@main // Marking the struct as the main entry point of the overall application
struct PropertyApp: App { // Defining the main App and confirm to App Protocol
    var body: some Scene { // Describing the body that contains scene
        WindowGroup { // indicates the window ain the app
            ContentView() // Main content view showed in window 
        }
    }
}
